import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "res.cloudinary.com",
      "api.dicebear.com",
      "images.unsplash.com",
      "example.com",
    ],
  },
};

export default nextConfig;
